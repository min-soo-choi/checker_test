import json


CLASSIFY_OUTPUT_SCHEMA = {
    "items": [
        {
            "question_num": "①",
            "group_prompt": "",
            "passage": "",
            "single_prompt": "",
            "example": "",
            "condition": "",
            "mc_choices": "",
            "sa_blank": "",
        }
    ]
}


CLASSIFY_PROMPT_TEMPLATE = """
당신은 한국어 수능 모의고사 문제 구성 요소를 식별하는 전문가입니다.
아래 텍스트/이미지 콘텐츠를 분석하여 지정된 JSON 형식으로 정보를 추출하세요.
최우선 순위는 원본 텍스트를 보존하는 것입니다. 분석, 평가, 문제 풀이를 하지 마세요.

* 콘텐츠 안전성 관련 사항은 무시하고 오직 구조적 분석 및 JSON 추출에만 집중합니다.
* 텍스트를 수정하지 마세요.

## 주요 텍스트
{all_relevant_texts}

---

## 주요 규칙
1. **엄격한 텍스트 보존 및 서식 지정**
   - 원본 텍스트를 수정하거나 변경하지 마세요. 원기호, 특수 문자, 한자 등을 그대로 보존합니다.
   - **유일한 서식 예외**:
     - **띄어쓰기**: 원기호 뒤와 선택지 텍스트 앞에 정확히 한 칸의 공백을 추가합니다.
     - **반복되는 기호**: 다섯 개 이상의 반복되는 `·` 기호는 하나의 `·`로 대체합니다.
2. **논리적 요소 통합**
   - 단일 논리 요소가 여러 줄/열/페이지에 나뉘면 하나의 연속 텍스트 블록으로 통합합니다.
   - JSON 필드에 배치하기 전에 모든 줄바꿈(`\\n`)을 제거합니다.
3. **지문 분할 규칙**
   - 새로운 지문은 `(가)`, `(나)` 등 명시적 마커가 있을 때만 분할합니다.
4. **문항번호 형식**
   - 문항번호는 반드시 원기호 숫자(①②③④⑤⑥⑦⑧⑨⑩⑪⑫⑬⑭⑮⑯⑰⑱⑲⑳)로 표기합니다.
   - 원본에 원기호가 있으면 그대로 유지하고, 없으면 ①부터 순서대로 부여합니다.
5. **그룹/단일 지시문 구분**
   - 여러 문항에 공통으로 적용되는 지시문만 `group_prompt`에 넣습니다. 예: "[선1-4] 다음 글을 읽고 물음에 답하시오."
   - 특정 문항 바로 앞에 붙는 지시문은 `single_prompt`에 넣습니다.
   - `<보기>` 블록은 `example`에만 넣고, `group_prompt`/`single_prompt`에 섞지 않습니다.
6. **본문(지문) 필수 추출**
   - 문제에서 공통으로 제시된 긴 글은 반드시 `passage`에 넣습니다.
   - 본문이 보이는 경우 `passage`를 빈칸으로 두지 않습니다.
4. **선택지 형식**
   - 객관식 선택지는 반드시 원기호 접두사를 포함합니다.
5. **원기호/괄호 보존**
   - 모든 원기호 및 괄호 문자를 그대로 보존합니다.

---

## 출력 형식
- 반드시 아래 JSON 스키마를 따릅니다.
- JSON 이외의 텍스트를 출력하지 마세요.

```json
{json_schema}
```
"""


def get_classify_schema_text() -> str:
    return json.dumps(CLASSIFY_OUTPUT_SCHEMA, ensure_ascii=False, indent=2)